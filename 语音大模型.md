##  总体概念

火山引擎的语音合成（TTS）提供 **两种调用方式**：

1. **WebSocket（流式）**：文本发过去 → 服务端边合成边返回音频，适合长文本、低延迟场景。
2. **HTTP（非流式）**：文本发过去 → 服务端合成完一次性返回整个音频，简单，但延迟较高。

官方推荐 **V3 WebSocket**，大模型音色延迟更优。

------

## 🔹 接口地址

- **V1（旧版）**
  - WebSocket 单向流式：`wss://openspeech.bytedance.com/api/v1/tts/ws_binary`
  - HTTP 非流式：`https://openspeech.bytedance.com/api/v1/tts`
- **V3（推荐）**
  - WebSocket 单向流式：`wss://openspeech.bytedance.com/api/v3/tts/unidirectional/stream`
  - WebSocket 双向流式：`wss://openspeech.bytedance.com/api/v3/tts/bidirection`
  - HTTP 单向流式：`https://openspeech.bytedance.com/api/v3/tts/unidirectional`

------

## 🔹 鉴权

- 使用 **Bearer Token**，格式必须是：

  ```
  Authorization: Bearer;{token}
  ```

  注意是 **分号 ;** 不是冒号 `:`。

- `appid` 和 `token` 在请求体里也要带上。

------

## 🔹 参数配置（通用）

### 1. app

- `appid`：应用 ID（必填）
- `token`：Access Token（必填）
- `cluster`：一般是 `"volcano_tts"`

### 2. user

- `uid`：用户 ID（必填，可随意填，只要非空）

### 3. audio

- `voice_type`：音色 ID（必填，如 `"zh_female_cancan_mars_bigtts"`）

- `encoding`：音频编码（pcm / wav / mp3 / ogg_opus）

  > 注意：wav **不支持流式**

- `speed_ratio`：语速，默认 1.0，可调 [0.8, 2]

- `rate`：采样率，默认 24000

- `emotion` / `enable_emotion`：部分音色支持情感（如“愤怒”、“开心”）

- `loudness_ratio`：音量 [0.5, 2]

### 4. request

- `reqid`：请求 ID（必填，必须唯一，建议 `uuid`）
- `text`：要合成的文本（必填，≤1024字节，推荐 ≤300 字符）
- `operation`：
  - `query` → 非流式（HTTP 必须用这个）
  - `submit` → 流式（WebSocket 必须用这个）

------

## 🔹 WebSocket 特点

- **单条连接只能合成一次**。想多次调用 → 重新建连接。
- 返回的是分段音频（sequence）。最后一条消息 `sequence < 0`，表示结束。
- 音频是二进制（raw bytes），拼接起来即可得到完整音频。

------

## 🔹 HTTP 特点

- 一次请求 → 等待 → 返回完整音频（base64 编码）。
- 你需要手动 `base64` 解码后保存为 `.wav/.mp3` 文件。

------

## 🔹 错误码常见情况

- **3000**：成功
- **3001**：参数错误（比如 `operation` 写错）
- **3010**：文本超长
- **3011**：无效文本（空文本/全是符号/语种不匹配）
- **3050**：音色不存在（`voice_type` 写错）
- **quota exceeded**：额度用完，需要购买正式版

------

## 🔹 开发重点

1. 推荐使用 **V3 WebSocket**，低延迟。
2. `appid`、`token`、`voice_type` 必须正确。
3. 每次调用 `reqid` 不能重复。
4. WebSocket：收到音频段按顺序拼接；HTTP：解码 base64 即可。
5. 不支持 `"豆包语音合成模型2.0"` 的音色（比如 `"zh_female_vv_uranus_bigtts"`）。



```
python3 examples/volcengine/binary.py --appid <appid> --access_token <access_token> --voice_type <voice_type> --text "你好，我是火山引擎的语音合成服务。这是一个美好的旅程。"


python3 examples/volcengine/binary.py --appid 2434296829 --access_token rLv47-O5whQ6vtiwCZ7AonO-I31I-9no --voice_type en_male_adam_mars_bigtts --text "Have you heard the Chinese idiom Sai Weng Lost His Horse?"
```

